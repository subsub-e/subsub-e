def merge(left,right):
    def loop(left,right,ss):
        if not (left == [] or right == []):
            if left[0] <= right[0]:
                ss.append(None) # replace None with proper code
                return None # replace None with proper code
            else:
                ss.append(None) # replace None with proper code
                return None # replace None with proper code
        else:
            return None # replace None with proper code
    return loop(left,right,[])